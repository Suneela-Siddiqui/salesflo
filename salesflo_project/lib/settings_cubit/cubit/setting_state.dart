import 'package:flutter/material.dart';


// we have created Setting State class 
// This will store theme data as one of the property but there can be more than one property too
// used instead of theme data 


class SettingState{
  final ThemeData theme;
  SettingState({
    required this.theme
  });
}